typealias(hal_drm_clearkey)
typealias(hal_drm_clearkey_exec)
typealiasactual(hal_drm_clearkey)
typealiasactual(hal_drm_clearkey_exec)
init_daemon_domain(vendor_hal_drm_clearkey)
vndbinder_use(vendor_hal_drm_clearkey)
hal_server_domain(vendor_hal_drm_clearkey, hal_drm)
type vendor_hal_drm_clearkey, domain;
type vendor_hal_drm_clearkey_exec, exec_type, file_type, vendor_file_type;
